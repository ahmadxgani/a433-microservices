services:
  item-app:
    image: "ghcr.io/ahmadxgani/item-app:v1"
    ports:
      - "80:8080"
    depends_on:
      - item-db
    restart: "always"
  item-db:
    image: "mongo:3"
    restart: "always"
    volumes:
      - app-db:/data/db
volumes:
  app-db:
